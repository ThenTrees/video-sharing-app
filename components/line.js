import { View } from "react-native";

export default Line = () => {
    return (
        <View
            style={{
                width: "100%",
                height: 1,
                backgroundColor: "#ccc",
                marginBottom: 10,
            }}
        ></View>
    );
};
